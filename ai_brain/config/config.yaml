brain:
  name: "AI Brain"
  reflection_interval: 60        # seconds between brain loops
  max_thoughts_per_cycle: 3      # max reasoning steps per loop
  verbose: true

agents:
  max_parallel: 5
  timeout: 300                   # seconds before agent is killed
  retry_attempts: 2

memory:
  database: "brain.db"
  max_events_recalled: 20        # events returned per recall
  max_knowledge_recalled: 10

llm:
  provider: "anthropic"          # anthropic | openai | ollama
  model: "claude-haiku-4-5-20251001"   # lightweight for Pi
  temperature: 0.7
  max_tokens: 2048
  # For Ollama (local):
  # provider: ollama
  # model: mistral
  # base_url: http://localhost:11434

scheduler:
  check_interval: 10             # seconds between scheduler ticks

tools:
  web_search:
    enabled: true
    max_results: 5
  code_runner:
    enabled: true
    timeout: 30
    allowed_languages:
      - python
      - bash
  git:
    enabled: true

skills_dir: "skills"             # relative to ai_brain/
